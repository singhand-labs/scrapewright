# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

Scrapewright is an LLM-powered web scraping platform built as a Chrome Extension (Manifest V3) with a Node.js HTTP background service. The host runs as a per-OS service (systemd user unit / launchd LaunchAgent / Windows scheduled task at logon) installed by `./bin/scrapewright install`. External programs call configurable scraping services via a local HTTP API. The extension provides an AI-guided interactive wizard for service creation.

A **service** is a *step graph* (a small state machine of named steps), not a single script. Each step runs a scraping snippet in a sandbox; `onSuccess`/`onFailure` edges route control between steps to express loops, branches, and pagination. See "Step Graph model" below.

**Tech stack:** Chrome Extension MV3, Vanilla JS, Node.js >= 18. `node --test` (no Jest). Root `package.json` only provides `jsdom` (used by extension tests).

## Common Commands

```bash
# === Unified CLI (recommended entrypoint) — ./bin/scrapewright ===
./bin/scrapewright install [--port=N]   # install OS service (default port 8765)
./bin/scrapewright install --no-autostart   # install without auto-start at login
./bin/scrapewright status               # service state + /health
./bin/scrapewright doctor               # full diagnostic (exits non-zero if sick)
./bin/scrapewright start                # start the service
./bin/scrapewright stop                 # stop the service
./bin/scrapewright restart              # restart service (picks up host.js changes)
./bin/scrapewright run [--port=N]       # foreground run (debugging, log-watching)
./bin/scrapewright logs -f              # tail host log
./bin/scrapewright throttle on|off|status  # toggle Chrome launch flags that disable renderer throttling (lazy-load sites)
./bin/scrapewright uninstall            # stop + remove OS service

# === Native host tests ===
cd native-host && npm test                       # node --test test/*.test.js
cd native-host && node --test test/host.test.js  # single file

# === Extension tests (need jsdom from root) ===
npm install                            # once, at repo root, for jsdom
cd extension && node --test test/*.test.js lib/*.test.js   # all
cd extension && node --test test/step-orchestrator.test.js # single file
# Note: tests live in BOTH extension/test/ and extension/lib/ (e.g. lib/service-registry.test.js).
# They require via relative paths ('../lib/...'), so run node --test from extension/.

# === Run host manually (dev mode; uses HTTP long-polling transport) ===
cd native-host && node host.js
node host.js --port=19880              # or SCRAPEWRIGHT_PORT=19880 node host.js
curl http://localhost:8765/health      # health check (no auth)

# === Inspect host log ===
tail -f ~/Library/Logs/scrapewright/host.log         # macOS
tail -f ~/.cache/scrapewright/host.log               # Linux
# Windows (PowerShell):
#   Get-Content -Wait "$env:LOCALAPPDATA\scrapewright\host.log" -Tail 20
# Override path with: SCRAPEWRIGHT_LOG_FILE=/path/to.log node host.js
# Boot crashes (before logger init) land in startup-error.log next to host.log.
```

After modifying extension files, reload the extension at `chrome://extensions/` (click the refresh icon on the extension card). Host-side changes require `./bin/scrapewright restart` to pick up. The extension's options page (`options.html`) shows a **Host Status** card with the current connection state (Connected / Disconnected), the last error, the log file path, a Reconnect button, and a Copy Diagnostics button — check there first when investigating connectivity issues.

`README.md` (English, default) and `README.zh-CN.md` (Chinese) are the canonical user-facing references — both document the full HTTP API, DSL, distributed deployment, and CDP comparison. `docs/technical-whitepaper.md` (中文) and `docs/technical-whitepaper.en.md` (English) are the design whitepaper — architecture, data flow, module reference, and extension guide. `req.md` is the original requirements spec (local only).

`examples/` holds concrete reference service definitions (`baidu4.json`, `yuanbao.json`) — full persisted service objects including `steps`, `inputSchema`, `outputSchema`, and `config`. These are the best examples of the step-graph DSL in the wild; read one before authoring or debugging a service.

## Service install internals

`./bin/scrapewright install [--port=N] [--no-autostart]` writes a per-OS service file via `native-host/lib/service-install/`:

- **Linux:** systemd user unit at `~/.config/systemd/user/scrapewright.service`. Auto-start via `systemctl --user enable --now scrapewright` + `loginctl enable-linger <user>` (so the user manager runs at boot).
- **macOS:** launchd LaunchAgent at `~/Library/LaunchAgents/com.scrapewright.host.plist`. Auto-start via `launchctl bootstrap gui/<uid> <plist>`. `KeepAlive=true` restarts on crash.
- **Windows:** scheduled task `ScrapewrightHost` with `-AtLogOn` trigger. Registered via PowerShell `Register-ScheduledTask`. No admin required.

Each service file embeds three things at install time: absolute node path (resolved by `lib/service-install/locate-node.js` via `process.execPath`), absolute host.js path, and the port as `--port=N` arg. So a user running `scrapewright install --port=9123` gets a service pinned to that port.

**Migration safety net:** `scrapewright doctor` and `scrapewright install` detect and remove any legacy Native Messaging artifacts (manifest JSON on Linux/macOS, registry key on Windows) via `native-host/lib/migration.js`. Always prints a one-line notice — never silent.

**Diagnostic order for connection failures:**
1. **Extension options page** → Host Status card. Red badge = host unreachable.
2. `./bin/scrapewright status` — service installed? `/health` reachable?
3. `./bin/scrapewright doctor` — full diagnostics (service, host, migration state, port match).
4. `./bin/scrapewright logs -f` — host's view of incoming requests.

The host writes structured logs (ISO timestamp + level + message + JSON fields) to both stderr and the log file. The log file is the lifeline when the OS service supervisor launches the host — stderr goes to the journal/launchd log. A synchronous boot trap at the top of `host.js` catches crashes *before* the logger initializes and writes them to `startup-error.log`.

The background service worker tracks `nativeState` (mode, lastError, connected/disconnected timestamps, reconnect attempts) in `chrome.storage.local`, updated at every connection transition. `mode` is `'polling'` (extension connected via long-poll) or `'disconnected'`; the legacy `'native'` value is migrated to `'polling'` on load. The options page polls `GET_NATIVE_STATUS` every 3 seconds. `RECONNECT_NATIVE` resets the polling flag and calls `initCommunication()` again.

## Scrape-tab throttling (five-layer stack)

Scrape tabs are opened as **background tabs** by default (`chrome.tabs.create({active:false})`) so the user's keyboard focus is never stolen. For most sites this is fine. For `IntersectionObserver`-driven lazy-load sites (Facebook feeds, infinite scroll, virtualized lists), background tabs hit Chrome's renderer-level frame-production throttle — no compositor frame means no IO callback means no lazy-load. Five stacked layers address distinct throttle/filter mechanisms:

1. **visibility-keepalive** (`lib/visibility-keepalive.js`, default on): injects a MAIN-world override of `document.visibilityState='visible'` + rAF keep-alive. Fixes page-JS that gates further loading on its own visibility check. Does NOT cause Chrome to produce compositor frames.
2. **Enhanced Scraping Mode** (`lib/renderer-activation.js`, opt-in via options-page toggle backed by `chrome.storage.local` flag `enhancedModeEnabled`): gates availability of the **trusted-wheel fallback** (layer 4 below). Previously also ran `Page.setWebLifecycleState` via `chrome.debugger` (RC18 Plan A); removed in RC20 as redundant — brief tab activation (layer 5) makes lifecycle naturally ACTIVE during the input window. The `debugger` permission is in required `permissions` (Chrome silently strips it from `optional_permissions`).
3. **Chrome launch flags** (necessary but not sufficient for IO-driven lazy-load): `scrapewright throttle on` (`native-host/lib/throttle-config/`) rewrites the Chrome launcher per-OS (Linux `.desktop`, macOS wrapper AppleScript app at `~/Applications/Chrome-Scrapewright.app`, Windows `.lnk` shortcuts) to add `--disable-background-timer-throttling --disable-backgrounding-occluded-windows --disable-renderer-backgrounding --disable-features=CalculateNativeWinOcclusion`. Requires Chrome restart. Use temp dirs in tests, never live user files.
4. **Trusted-wheel fallback** (RC19, `lib/renderer-activation.js` + `lib/scroll-ops.js`, opt-in via Enhanced Mode): when programmatic `scrollBy` stalls (no content growth), `scroll-ops.js` calls `dispatchTrustedWheelScroll` which transiently attaches `chrome.debugger` and issues CDP `Input.dispatchMouseEvent({type:'mouseWheel', deltaY})`. CDP input enters Chrome through the same input pipeline as real OS-level input, so the resulting wheel event has `event.isTrusted=true` — the **only** programmatic mechanism that produces a trusted wheel event. Required for sites whose lazy-load loader filters non-trusted scroll events (programmatic `scrollBy` produces `isTrusted=false` and is filtered out). Relay chain: `content-script.js` → `chrome.runtime.sendMessage(TRUSTED_WHEEL_SCROLL_REQUEST)` → `background.js` → `RendererActivation.dispatchTrustedWheelScroll(sender.tab.id)`. Detection-risk: only `Input.*` CDP commands added (no `Runtime.*`/`Network.*`/`DOM.*`). Capped at `DEFAULT_MAX_TRUSTED_WHEEL_ATTEMPTS=3` invocations per `$scrollToBottom`. Universality: no site-specific logic in prompts or DSL — the LLM keeps writing `$scrollToBottom`, infrastructure dispatches trusted wheel events transparently when programmatic scroll stalls.
5. **Sticky tab activation** (RC20 origin, RC56 sticky rewrite, RC64 `$openTab` coverage, `lib/tab-activation.js`, default on): addresses Chrome's hard architectural rule — compositor frames are produced *only* for the active tab in the focused window, so both IO callbacks AND CDP Input.dispatchMouseEvent require activation. `TabActivation.requestActivation(tabId)` activates the scrape tab and **KEEPS it active** (RC20's per-op restore caused activate/restore churn on back-to-back ops); the next op simply re-activates if the user switched away. **Window-focus enforcement (forty-ninth log, user-authorized):** a tab can be the active tab of its window while its WINDOW lost OS focus (user working elsewhere; occluded or minimized window) — no compositor frames either way, so `requestActivation` now ALSO re-asserts window focus via `chrome.windows.update({focused:true, state:'normal'})` whenever the scrape window is not the last-focused window OR the content script reports the page is hidden/unfocused (`needsWindowFocus` — real `document.visibilityState`/`hasFocus()` read from the ISOLATED world, where the MAIN-world visibility-keepalive override cannot lie; this closes the occluded/other-app-holds-focus case Chrome's own window APIs cannot see). The result carries `crossWindow:true` / `focusedWindow:true` for diagnostics. This supersedes the thirteenth-log within-window-only design per the user's explicit authorization that correct execution takes priority over their manual focus. The pre-2026-09 refusal starved fresh verify tabs of compositor frames forever (page stayed an unrendered shell). The user's last-clicked tab is tracked via `chrome.tabs.onActivated` with a suppression Set (tabIds of our own pending `tabs.update`, cleared on the matching event or a 1000ms safety timer); when a tab closes while active (scrape-tab auto-close), focus lands on the last-clicked tab via `chrome.tabs.update`, focusing its window too if different. State persists to `chrome.storage.session` (the MV3 SW can suspend during page-context LLM calls). Listeners register at SW top level (`background.js` `initTabActivationListeners()`). Activation sites: (a) main-tab ops `domScrollToBottom`, `domHover`, `hoverDismiss`, and (forty-ninth log) the previously-uncovered scroll/click family `domScrollBy`, `domScrollIntoView`, `domWaitForStable`, `domClickInList` — RC20 wrapped only the ops it knew about and the forty-ninth-log verify scroll loop used exactly the unwrapped ones, so a user switching away froze the feed (count stuck at 2, then 8, across 18 iterations) with no re-activation ever attempted — all via `content-script.js withTabActivation(label, fn)` sending `TAB_ACTIVATION_REQUEST` only (sticky: no release message); (b) RC64 — `$openTab` sub-tabs: `background.js handleOpenTabExecute` calls `TabActivation.requestActivation(tab.id)` directly right after `createScrapeTab`, BEFORE `waitForTabLoad`, so JS-heavy detail pages render with compositor frames from initial load onward (without it the app shell mounts — `$wait` resolves — but content never renders and every extract comes back deterministically empty; guarded by `test/tab-activation-drift.test.js`). Universality: pure infrastructure, no site-specific logic, no prompt changes.

The popup-window path (`chrome.windows.create({type:'popup', focused:false})` from RC12/RC17) was removed in RC20 — brief tab activation makes background tabs work without the focus-stealing cost of popups. `closeScrapeTab(tab)` is now a thin idempotent wrapper over `chrome.tabs.remove`.

## High-Level Architecture

### Two-Process Design

```
External Program
      |
      | HTTP POST /api/v1/services/{name}/execute   (X-API-Key header)
      v
+-------------+   HTTP long-polling                 +------------------+
|  host.js    | <=================================> | background.js    |
|  (Node.js   |   /api/v1/extension/poll            | (Service Worker) |
|   HTTP srv) |   /api/v1/extension/response        +--------+---------+
+-------------+                                             |
                                                    chrome.tabs / chrome.scripting
                                                    / offscreen messaging
                                                            v
                                                  +------------------+
                                                  | offscreen.js     |
                                                  |  (owns sandbox   |
                                                  |   iframe +       |
                                                  |   tabIdStack)    |
                                                  +--------+---------+
                                                           | postMessage
                                                           v
                                                  +------------------+
                                                  | sandbox.html     |
                                                  |  (eval allowed)  |
                                                  +------------------+
```

1. **Host** (`native-host/host.js`): HTTP server (default `:8765`, `SCRAPEWRIGHT_PORT`/`--port=`) that forwards requests to the extension via HTTP long-polling (`GET /api/v1/extension/poll` for delivery, `POST /api/v1/extension/response` for replies). Authenticates external requests with `X-API-Key` → `SCRAPEWRIGHT_API_KEY` env (default `dev-key`).
2. **Background** (`extension/background.js`): Service Worker. Owns the `ExecutionQueue` that serializes all service calls. Orchestrates scraping via `StepOrchestrator` (step graph), opens tabs, runs steps, retries on failure, AI auto-fix. Maintains the transport to the host.
3. **Offscreen** (`extension/offscreen.js` + `lib/offscreen-executor.js`): The **primary** script execution surface. `OffscreenExecutor` ensures a single offscreen document exists (`chrome.offscreen`), which itself hosts the sandbox iframe and a `tabIdStack` so concurrent-looking DOM requests route back to the originating tab.
4. **Sandbox** (`extension/sandbox.html` + `sandbox.js`): Declared sandbox page in `manifest.json`. The only place where `new Function()`/`eval()` is allowed (MV3 CSP). Scripts execute here; `$` API calls are forwarded up to the offscreen doc, then to background, then to the content script of the target tab.
5. **Content Script** (`extension/content-script.js`): Injected into all pages (`document_idle`). Performs the actual DOM operations, element annotation, and DOM snapshot capture. Its own script-execution path (`ScriptExecutor` + per-tab sandbox iframe) has **no callers** — `$openTab` fn bodies execute via a fresh `OffscreenExecutor` bound to the sub-tab; the content script only relays the `openTab` request from the main tab and serves the sub-tab's `$` API calls.

**HTTP-only transport.** `background.js: initCommunication()` probes `GET /api/v1/extension/poll` over HTTP; if the host is reachable, it uses **HTTP long-polling** (extension pulls requests via `poll`, replies via `POST /api/v1/extension/response`). There is no other transport. The host runs as an OS service (Linux systemd / macOS launchd / Windows scheduled task) installed by `./bin/scrapewright install`, so it's always available when the user is logged in.

### Step Graph model (the core mental model)

A service is defined by `steps: [...]`, each step:

```js
{ id, name, script, condition?, onSuccess, onFailure?, maxIterations? }
```

- `script` — a scraping snippet (see DSL below). Empty/`// PENDING_ANNOTATION` steps are allowed during authoring but block deploy.
- `onSuccess` — the id of the next step to run when this step **succeeds** (content ready / data extracted), or `TERMINATE` (stop). For a wait/poll step, point this at the extraction step that runs once content is ready.
- `onFailure` — the id of the next step when this step **fails or gives up** (its `condition` is false, its retry budget is exhausted, or it returned `{ failed:true }`/`{ error:'...' }`), or `TERMINATE`. Used for branches, error paths, and poll-exhaustion.
- `condition` — optional JS expression evaluated in the target tab (via `chrome.scripting.executeScript`); if false, the step is skipped and `onFailure` is followed.
- `maxIterations` — how many times a step may run. `1` (default) = a normal step that runs once. **`>1` opts the step into retry/poll semantics.** `StepOrchestrator` **auto-boosts** it to the global cap (`config.maxStepIterations`, default 50) for any step that is the target of a back-edge, so legitimate loops aren't prematurely killed.

> **No `SELF` sentinel.** Earlier versions used `onSuccess: 'SELF'` for self-loops with a counterintuitive convention (`{done:true}` exited via `onFailure`). This is **removed**. Polling/retry is now expressed by `maxIterations>1` + returning `{done:false}`. `validateChain` rejects any lingering `onSuccess:'SELF'` loudly. Legacy services must be reconfigured.

`StepOrchestrator` (`extension/lib/step-orchestrator.js`) executes the graph: create tab → walk steps following edges → collect `{ stepId, result, snapshot }[]` → return `{ finalResult, steps }`. Result signals (only inspected when `maxIterations>1`; a normal step's result is pure data and always follows `onSuccess`):
- **Not-ready** → retry the same step: `{ done:false }`, `{ ready:false }`, `{ generating:true }`, `{ loading:true }`, etc. (up to `maxIterations`, then `onFailure`).
- **Ready/data/`{done:true}`** → follow `onSuccess`.
- **Failure** → follow `onFailure`: `{ failed:true }`, `{ error:'msg' }`.

A misconfigured poll step (e.g. `onSuccess:'<next>'` but `maxIterations<=1`) will run once and advance without retrying — a visible, debuggable failure rather than a silent mis-execute. Data flows between steps via the script globals `__stepResults__` (map of prior results by step id) and `__lastResult__` (previous step's result), injected by the orchestrator into each step's input — `__lastResult__` carries state across a step's own retries, so list-iteration can be a single self-polling step.

The step chain topology is validated everywhere services are persisted (`ServiceRegistry.save` calls `wizard-utils.validateChain`): every `onSuccess`/`onFailure` target must exist, no orphans, no duplicate ids, and no `SELF`. Adding/removing/reordering steps must **relink** the chain (`wizard-utils.relinkChainToArray` + `appendStepWithChainLink`) — a naive array edit silently breaks execution, which is what the recent `relinkChainToArray` fix addressed.

### Script Execution Flow (offscreen path)

1. `background.js` creates a tab with the target URL, waits for `status === 'complete'`. `waitForTabLoad` (copies in `background.js` AND `wizard.js` — keep in sync) probes `document.readyState` + body text length via `chrome.scripting` 1.5s before its 120s hard timeout (sixtieth-round user directive: slow-network tolerance — was 60s; early enough to beat the outer `withTimeout(120s)` wrappers in `makeWizardRail`/`verify-runner`; tab-create windows are 15s each with one retry = up to 30s, and the `$openTab` sub-tab execution budget is 120s) and resolves when interactive/complete with >1000 body chars — streaming pages can render fully (3.5M chars) without ever firing `complete` (forty-fifth log); the probe can only upgrade the outcome, and the hard reject embeds the probe evidence (`readyState=…, bodyChars=…`).
2. `StepOrchestrator` calls `deps.executeScript(tabId, stepScript, enrichedInput, timeoutMs)`.
3. `OffscreenExecutor` ensures the offscreen document, wraps the script, and sends `EXECUTE_SCRIPT_OFFSCREEN` (with `tabId` pushed onto offscreen's `tabIdStack`).
4. `offscreen.js` forwards `EXECUTE` to its sandbox iframe via `postMessage`.
5. `sandbox.js` runs `new Function('__input__','__stepResults__','__lastResult__', 'return <script>')(input, stepResults, lastResult)` — the `<script>` arrives pre-wrapped by `OffscreenExecutor.wrapScript` as `(async function(__input__) { <step script> })(__input__)`, i.e. step scripts are async-function BODIES (top-level `await`/`return` work directly). `$` API calls become `DOM_REQUEST` messages back to offscreen. A `SyntaxError` at construction (Function-constructor errors carry no position) is enriched by a prefix-scan locator: the error message gains `SYNTAX_ERROR: <msg> at line L, column C of the step script ... Near: <snippet with >>><<< marker>` so the model and diag.read can see WHERE a one-char typo lives (twentieth-log fix).
6. Offscreen relays `DOM_REQUEST` to background → `chrome.tabs.sendMessage` to the target tab's content script → content script performs the DOM op → `DOM_RESPONSE` flows back (offscreen dedupes by request id; content-script replies reach it directly *and* via a background rebroadcast).
7. On completion, sandbox posts `EXECUTE_RESULT`; offscreen pops the `tabId` and sends `SCRIPT_RESULT` (`_fromOffscreen`) to background. Timeouts send `EXECUTE_SCRIPT_TIMEOUT` so the stack is cleaned up.

**`$openTab` path**: `sandbox.js $openTab` → `DOM_REQUEST('openTab')` → background → main tab's content script (`domOpenTab`) relays `OPEN_TAB_EXECUTE` → background `handleOpenTabExecute` opens the sub-tab and runs the fn body through a **fresh `OffscreenExecutor` bound to the sub-tab** (activation before load, 60s local timeout, sub-tab snapshot captured pre-destroy on both paths). `TAB_RESULT.result` carries the fn body's return value — unwrapped from the executor's `{result, selectorDiagnostics}` envelope (seventeenth-log fix; the day-one leak made `$openTab` resolve the envelope, silently degrading every caller including the shipped examples). The legacy `ScriptExecutor` + content-script sandbox-iframe execution path has no callers.

### Script DSL ($ API)

User scripts (LLM-generated) run as `return <expr>` in the sandbox with these async globals — 20 primitives. Full table with error shapes: `docs/technical-whitepaper.md` §7.2. Selector diagnostics (`_diagnostics`) are injected at the source of every list/extract/hover call and relayed up on success AND error paths (`DOM_RESPONSE` → offscreen → sandbox → `EXECUTE_RESULT.selectorDiagnostics`) for autoFix. When a container selector matches **zero**, `$extractList`/`$extractListMulti`/`$extractWithHover` compute a live **selector differential** (`computeSelectorDifferential` in `content-script.js`): trailing `:not()`/`:has()` clauses are stripped right-to-left and each stage counted in-page — commas INSIDE functional pseudo-class arguments (`:has(a[x], a[y])` — the standard inclusion-list shape) participate; only TOP-LEVEL commas (union lists) bail — the error message embeds `SELECTOR DIFFERENTIAL: sel → N; ...` naming the clause that removed the whole population, and verify-runner reroutes the generic INPUT_VALUE_SUSPECT advice to SELECTOR_OVERFILTERED whenever the stripped base still matches >0 — on red THROW runs too (the zero-container throw shape emits `STEP_FAILED` with the sandbox-relayed diagnostics, and the verify error branch runs the container census + embeds the lines, fifty-first log). When the selector matches **but carries strippable trailing clauses** (`attachClauseCostCensus`, forty-fourth log), the populated-path census also carries `selectorDifferential` + a clause-cost note quantifying how many base matches the exclusion clauses removed — so a partially-overfiltering exclusion (attr-NAME-based `:has([data-*])` design-system attributes often match organic items too) is auditable instead of silent.

**Reads** — `$(sel)` waits for an element (30s) and returns ElementData (`textContent` capped at 50000 chars). `$count(sel)` / `$list(sel)` count/collect across the main doc + same-origin iframes; invalid selectors throw (no silent 0). `$extract(sel, attr?, timeoutMs?)` reads text or an attribute (`outerHTML`/`innerHTML` are DOM properties, not attributes; fail-fast default 5s, third param overrides; an absent attribute yields null + an `attrAbsent` diagnostic). `$labelledby(sel, attr?, timeoutMs?)` resolves an ARIA reference attribute (`aria-labelledby` default, `aria-describedby` opt-in) — returns `{text, attr, refCount, missingIds?, note?, viaDescendant?}` where `.text` concatenates the referenced elements' text, reading values that exist only in hidden-but-readable tooltip spans; when the match carries neither reference attribute, resolution descends to its first descendant that carries one (own attrs win; disclosed via `viaDescendant` + note — forty-third log); every empty path keeps the object shape and carries its falsification reason in `.note` (probe.labelledby returns the same shape — thirty-second log RC-C aligned the two after a probe↔DSL envelope mismatch made copied `.text` access structurally empty). `$check(sel, prop)` reads a property. `$exists(sel, timeoutMs?)` polls for a visible match (default 5s; `timeoutMs: 0` = one immediate query). `$wait(sel, ms?)` waits for a selector (30s cap) then sleeps `ms`; an empty selector throws `WAIT_SELECTOR_EMPTY`.

**Interaction** — `$click(sel, timeoutMs?)` / `$type(sel, text, timeoutMs?)`: element wait defaults 10s (override via the timeout param; reads keep 30s). `$type` coerces non-string `text` with `String()` and records the original type in `_diagnostics`. `$clickInList(containerSel, subSel, opts?)` clicks a sub-element in each container (`{clicked, errors[{index,reason}]}`, default `delayMs=500`; activation-wrapped like the scroll family — the post-click lazy hydration needs frames, forty-ninth log). `$waitForStable(sel, opts?)` polls textContent until stable (streaming-content completion, default 20000ms; activation-wrapped, and its not-stable diagnostics carry `pageState` — a hidden/unfocused page means the tab was throttled and the streaming content may never have arrived, forty-ninth log).

**Lists** — `$extractList(containerSel, fieldMap, opts?)` extracts one record per container (first-match per sub-selector; `opts.allowEmpty` waives the empty-throw; field spec `{selector, attr?, labelledby?, multi?}` — `labelledby:true`/`aria-describedby` resolves the ARIA reference on the match (with the same descendant fallback as `$labelledby`), the read for anti-scrambled textContent where the clean value lives only in the referenced hidden elements; thirty-third-log D1, forty-third-log descent; `multi:true` (forty-eighth log) returns an ARRAY of every match of that one field in document order — the same envelope `probe.extract {multi:true}` returns — while sibling fields stay first-match scalar. The asymmetry this closes: a fieldMap without `multi` yields a scalar string, and a script written against the probe's array shape (`for (const h of hrefs)` iterating links) silently iterates CHARACTERS over the scalar — dead code that ships empty while every gate reads the field as populated elsewhere). `$extractListMulti(containerSel, fieldMap, opts?)` returns arrays of ALL matches per field per container — equivalent to every fieldMap spec carrying `multi:true`; use it when the WHOLE call is multi, the per-field flag when only some fields are.

**Scroll** — all scroll ops are wrapped in sticky tab activation + window-focus enforcement (forty-ninth log: scroll-driven lazy-load needs compositor frames; a user switching away used to freeze the feed with no re-activation attempted). `$scrollBy(deltaY, selector?)` (when the window cannot move it falls back to the inner scrollable container, returning `fallback:"inner-container"` with coordinates on the root that actually scrolled — forty-sixth log: feeds that scroll inside an inner overflow container leave the window still, so window-only evidence proves nothing about feed exhaustion; a no-progress result carries `pageState` `{visibilityState, hasFocus}` read from the isolated world — real values the MAIN-world visibility-keepalive override cannot touch — and, when the page looks gated, `frameSample` `{rAFTicks, sampleMs}`: ~0 rAF ticks over ~300ms means the renderer stopped producing frames for the tab (lazy-load cannot fire; activation is automatic, `scrapewright throttle on` covers occlusion), normal ticks with a stable count means genuine exhaustion; forty-ninth log — the model previously rewrote the scroll step seven times with zero visibility evidence), `$scrollToBottom(selector?)` (incremental growth-probe loop; dual-signal stall: 3 no-progress iterations OR 3000ms without height growth; trusted-wheel fallback when stalled; returns `{scrolled, newY, newScrollHeight, stalled, stallReason, attempts, stallWindowMs}`), `$scrollIntoView(selector)`.

**Hover** — `$hover(anchorSel, popoverSel?, opts?)` dispatches a trusted mouseMoved at the anchor and captures the popover (`{hovered, htmlSnippet, popoverSelector, reason?, observedPopover?}`; default timeout 4500ms with a 3000ms no-signal early exit; `opts.index` selects the Nth match). `$extractWithHover(containerSel, fieldMap, opts)` is the container-scoped atomic extract+hover primitive (per-record `hovercards[]` with `anchorHref`/`anchorText` + `labelledbyText`/`labelledbyAttr`/`labelledbyNote` — the note now survives on empty harvests too, forty-fourth log; `anchorHref` resolves the nearest ENCLOSING `a[href]` via `closest()` when the anchor itself is not the link — anchorSel routinely matches the label span inside the link, whose own href is null, and hovercards[].url shipped empty on every record while the hover layer worked perfectly, sinking link-based classification (forty-ninth log; mirror kept in sync); `containerIndex`/`containerRange`/`maxContainers` split large batches). **Cold-tab timing (forty-fourth log):** field values are read BEFORE the hover batch, but on a cold tab the batch is exactly what hydrates lazily-mounted ARIA reference chains — so after the batch, `extractWithHoverRecords` re-reads `labelledby` fields whose value came back empty/undefined and fills from the post-hover DOM (non-empty values are never overwritten; non-`labelledby` fields are never re-read — attr/text reads don't hydrate from hovers). A labelledby field that reads empty in the records but `refResolved>0` in the same-run census is the pre-fix fingerprint; it now self-heals inside the primitive. **Dispatch failure + giant-DOM ticks (forty-fifth log):** a failed CDP dispatch (`hoverDispatched:false` — mouse never moved) skips the dwell loop AND the dismiss entirely (no popover can have mounted; the anchor-label harvest still runs) and returns the dispatch reason + a budgetNote naming it an environmental transient to retry — three 30s budget kills previously taught the model to abandon hovercards for the session. The dwell loop consumes `addedNodes` through a cursor into a candidate pool that persists across ticks (Set dedupe; RC43 two-tick stability preserved; detached nodes skipped), and the per-candidate loops bail mid-tick once past `deadline + TICK_DEADLINE_GRACE_MS(1500)` — a single scoring tick over a giant streaming DOM used to eat the caller's entire step budget with the deadline only checked between ticks; the result discloses `deadlineBailed:true` + a retry-with-larger-timeoutMs note.

**Tabs** — `$openTab(url, fn)` opens a new tab, runs `fn` in it via a fresh `OffscreenExecutor` bound to the sub-tab, and resolves to `fn`'s return value (60s local timeout; on failure the sub-tab's DOM is captured as `error.subTabSnapshot` before the tab is destroyed).

Plus the injected context globals `__input__`, `__stepResults__`, `__lastResult__` (see Step Graph model).

### HTTP API surface

All under `/api/v1`, all require `X-API-Key` header **except** `/health` and the two internal extension-bridge endpoints:

| Method | Path | Purpose |
|---|---|---|
| POST | `/services/{name}/execute` | Submit a job → returns `{ jobId }` |
| GET | `/jobs/{id}/wait?timeout=120` | Block until done (or timeout) |
| GET | `/jobs/{id}` | Job status |
| POST | `/jobs/{id}/cancel` | Cancel |
| GET | `/jobs` | List jobs |
| GET | `/services` | List configured services |
| POST | `/services/{name}/steps` | Add a step (agent-native parity) |
| PUT | `/services/{name}/steps/{stepId}` | Update a step |
| DELETE | `/services/{name}/steps/{stepId}` | Delete a step (relinks the chain) |
| GET | `/health` | Liveness (no auth) — for LB/k8s probes |
| GET/POST | `/extension/poll`, `/extension/response` | Internal host↔extension bridge (no auth) |

Routes are matched by regex in `host.js` (more-specific routes first). The external API is always async: `execute` returns a `jobId`, clients block on `wait`.

### Error Handling & Auto-Fix

`background.js` classifies errors:
- `ELEMENT_NOT_FOUND` / `SCRIPT_ERROR` → AI auto-fix (send error + DOM snapshot + step script to LLM, regenerate, retry — max 2 attempts)
- `LOGIN_REQUIRED` → fail fast with a helpful message
- Other errors → retry up to `config.maxRetries`

Snapshots are cleaned/sanitized for the LLM by `lib/dom-cleaner.js` (`cleanHtmlForLLM`), with snapshot budgeting in `lib/wizard-utils.js` (`truncateSnapshotForLLM`/`stripSnapshotsFromTestResult`). When a failure originates inside `$openTab`, the orchestrator prefers `error.subTabSnapshot` (the actual detail page) over re-capturing the main tab (usually the wrong page). Execution logs are stored in `chrome.storage.local` under `executionLogs` (max 100).

### LLM Integration

`extension/lib/llm-client.js` speaks two wire protocols: OpenAI-compatible `/chat/completions` AND the native Anthropic Messages API (`/v1/messages`). A per-config `apiProtocol` knob (Settings select: `auto`/`anthropic`/`openai`, default `auto`) governs the choice — auto PREFERS Anthropic Messages and falls back to OpenAI chat/completions when the server does not speak it (capability probed once per base URL, cached module-level: 404/405, or a 200 with an OpenAI-shaped body, pin the base to OpenAI; the openai-shaped 200 is parsed in place without a second request). The Messages path maps leading system messages to the top-level `system` param, normalizes `stop_reason` to OpenAI finish_reason vocabulary (`max_tokens`→`length`, `refusal`→`content_filter`), and sends the Claude Code agent client signature (`user-agent: claude-cli/... (external, cli)`, `x-app: cli`, `anthropic-version`, both `x-api-key` and `Authorization: Bearer`) so coding-plan lanes (GLM Coding Plan, etc. — quota honored only on their dedicated Base URL, e.g. `https://open.bigmodel.cn/api/coding/paas/v4`) classify the traffic into the agent lane. Supported providers: OpenAI, Moonshot, Kimi (Moonshot Platform), Anthropic, GLM (Zhipu AI, pay-as-you-go lane), GLM Coding Plan (Zhipu AI — same-company second preset defaulting to `https://open.bigmodel.cn/api/coding/paas/v4`, mirroring the moonshot/kimi two-lane pattern; per Zhipu's official coding-plan doc the plan quota is honored only on the coding and `/api/anthropic` endpoints, team-plan keys are not interchangeable with platform keys, and the third official lane — OpenAI Response `https://open.bigmodel.cn/api/v1` — is unsupported). Custom base URLs supported. Specific messages for 404 (check URL/model), 401/403 (check API key), and balance/recharge bodies (deterministic billing condition — non-retryable fail-fast naming the recharge/coding-plan-Base-URL remedies); gateways that wrap errors in HTTP-200 `{code, msg, success:false}` envelopes (bigmodel.cn observed) are decoded by body semantics — a 404-flavored `msg` on the Messages probe is a capability miss in auto mode (fall back to OpenAI), otherwise it surfaces the gateway's own diagnosis non-retryably with Base-URL/protocol guidance (35th log); request/response details logged to console (chunked one-line strings). The wizard (`wizard.js`, ~80KB) drives the interactive research-first service creation flow (user-visible stages: Requirements → AI Research → Review & Deploy; internal phase ids 1-5 retained) and is paired with `wizard-utils.js` for chain validation/manipulation. The research-session verify report (`lib/verify-runner.js`) layers data-driven field detectors over the green/red verdict — per-field empty ratio (each census entry carries `emptyRecordSamples` — WHICH records are empty, 1-based ordinal + a content hint; nested paths use `parentIndex.subIndex` — so "postId 2/4" names the exact records to re-probe; forty-eighth log), literal-only fields (staticLint), oversized fields, markup masquerade (`HTML_FIELD_NO_MARKUP`, report-only, with `copiedFrom` sibling provenance); every record-level census recurses into NESTED record arrays with `a.b[].c` paths (sub-field keys = schema-declared UNION data-present), the REQUIRED_FIELD_EMPTY gate resolves each path against the DECLARING array's `items.required` via `schemaItemRequiredForPath` and its message NAMES the empty record ordinals (forty-seventh log: `posts[].hoverCards[].type` hardcoded '' 9/9, 52-67K card `htmlSnippet`, and an `htmlSnippet`=content-slice masquerade all hid under one green verify when the census walked depth-1 only). Report-only censuses run even when another gate has flipped the run red (forty-eighth log: a CLICK_CONTAINERS_EMPTY red used to skip every census, so the round that most needed field evidence produced none), and a CLICK_CONTAINERS_EMPTY zero-match corroborated by a later same-run match on the identical container selector (`corroborateContainerZero` — mount timing, not a wrong selector) downgrades to the advisory `CLICK_CONTAINERS_TRANSIENT` tag without vetoing the run. Two more report-only censuses (forty-ninth log): `detectFrozenScrollCount` (a counter frozen at a NONZERO value across a trailing streak of ≥4 done-false iterations in a run that used scroll APIs — entry carries `frozenCount`/`streak`/`grewFrom`, tag `SCROLL_COUNT_FROZEN`; the frozen-ZERO trap stays the fourth-log detector's class; the note teaches reading the scroll diagnostics' pageState/frameSample to pick between renderer gating and genuine exhaustion instead of rewriting the scroll step) and `detectDuplicateIdValues` (an id-named field whose value repeats across records — entry carries the duplicated value + 1-based record ordinals, tag `DUPLICATE_ID_VALUES`; repeated identity values mean the extractor fell back to a container-level shared value such as the list owner id instead of a per-record identifier). Fiftieth log hardening on top: the frozen-scroll census only fires when the freeze is TERMINAL (a later done-true mention or any later mention at a higher count proves the freeze self-resolved — feed mounts come in bursts, and a tag on a green self-resolved run is noise that trains the model to ignore it) and events-only censuses now also run on orchestration-error runs (the whole census block used to hang off the no-error path, so the POLL_EXHAUSTED run carrying THE terminal frozen counter produced zero censuses); `detectSiblingCountContrast` (a count-named field empty on ≥60% of records while a sibling count-named field extracts real values from the same family — entry carries `populatedSibling`/`siblingSample` + the aria routes, tag `COUNT_FIELD_HIDDEN_VALUE`, report-only; likes-5/5-empty vs shares-5/5-populated was the incident — the family demonstrably renders counts, so the empty value lives in an aria-label attribute or an aria-labelledby reference, not textContent) and POLL_EXHAUSTED errors now embed iteration pacing computed from the step's own output timestamps (`paced N attempt(s) over Xms (avg Yms/attempt)`; avg <300ms adds the settle teaching — scrollBy resolves in tens of ms while lazy-load mounting needs ~1s, so back-to-back not-ready polls cannot grow the counter no matter the iteration budget). Fifty-second log on top: io.confirm and service.update reject `SCHEMA_STRAY_FIELD_DECLS` — field declarations (schema-shaped objects) sitting OUTSIDE `properties` are invisible to every schema-driven gate (the incident shipped `role: ''` hardcoded through seven artifact versions because hoverCards sat at the items level and the never-extracted lint's nested recursion never ran); `detectImplausibleTimeFields` (tag `TIME_FIELD_IMPLAUSIBLE`, report-only — a time-named field whose non-empty values carry no date shape ["m.meCat…"] is a labelledby read on the WRONG anchor; the census teaches filtering candidates by date shape); and verify.run fires a `STAGNANT_DISCLOSURES` advisory on the THIRD consecutive identical disclosure signature (same partial-empty paths/counts + junk fields) — teaching the three exits (probe the named records on the research tab / renegotiate via io.confirm / accept and disclose) with a probe-since-last-verify counter flagging bare update↔verify loops; an ALL-EMPTY signature never counts as stagnation (error classes changing between runs is progress, not a stuck loop — fifty-sixth log). `probe.timestamp` is the nineteenth tool: a composite that does the whole absolute-timestamp dance (candidate anchors → hover → labelledby/aria/text harvest → date-shape filtering via the exported `looksLikeDate` predicate, absolute preferred over relative) in ONE research call — the recurring postTime wall (logs 15/24/28/31/52/55/56) gets a tool, not more prompt rules. The verify tab-create path retries once on a 10s stall (fifty-sixth log: two consecutive stalls killed a 59-turn session's final verifies behind a failed verdict). service.update's static-lint receipt also names invented Playwright-style pseudo-classes (`:textish`/`:textless`/`:has-text`… — browsers reject them at querySelectorAll time; teach the attribute/structural rewrite; fifty-seventh log, second occurrence of the class), and the container-zero census text teaches the FAIL-SOFT artifact (`opts.allowEmpty` + `{posts:[], note}` — a service that reports honest empties instead of erroring on a proven-empty input class).`probe.loginState` (twentieth tool) grounds login-state claims in a marker census (password fields / login links / logout markers) — "requires login"/"unauthenticated" conclusions must quote it, never page shape (the fifty-first-log "no-login" claim was a misjudgment: the browser was logged in throughout). io.confirm and service.update reject `PLACEHOLDER_SCHEMA` — unfilled stub fields (`"type":"type"`, description `placeholder`/`TODO`/`…`) never reach the user's confirm panel (fifty-ninth log: the user had to reject a stub contract twice).  The time-related knowledge units (auto-attached on RELATIVE_TIMESTAMP/TIME_FIELD_IMPLAUSIBLE tags) now lead with probe.timestamp as the FIRST MOVE — the fifty-eighth log shipped both postTimes relative with the one-call tool sitting unused in the prompt. `detectLabelPrefixedCounts` (tag `LABEL_PREFIXED_COUNT`, report-only) names count fields shipping a control label PLUS the count ("Like: 37 people") with the parsed sample and the one-line parse teaching; the junk-field finish disclosure dedupes repeated census entries.

## Important Constraints

**MV3 CSP Compliance:** No `eval`/`new Function` in content scripts or service workers. All dynamic script execution must go through the sandbox page (now reached via the offscreen document). Never add inline event handlers (`onclick`) in HTML — use `addEventListener` in JS.

**Service install:** Run `./bin/scrapewright install` (Linux/macOS) or `.\bin\scrapewright.cmd install` (Windows). The CLI writes a per-OS service file (systemd unit / launchd plist / scheduled task) at install time, embedding the absolute node path, host.js path, and port. Custom port: `scrapewright install --port=N`. Run `scrapewright doctor` whenever connection fails — it surfaces service-not-installed, host-unreachable, port-mismatch, and legacy-artifact states.

**Cross-Platform:** Linux uses systemd user unit + `loginctl enable-linger`. macOS uses launchd LaunchAgent. Windows uses scheduled task at logon (no admin required). All three are per-user scope.

**Concurrency:** Only one service execution runs at a time. The `ExecutionQueue` in `background.js` serializes all calls. This is required because the offscreen document hosts a single shared sandbox iframe and `tabIdStack` — concurrent executions would route DOM requests to the wrong tab. For higher throughput, scale horizontally: run multiple host instances on different ports behind a load balancer (`deploy/scrapewright-manager.sh`, `deploy/Dockerfile`, `deploy/k8s.yaml`) — see README "Distributed deployment".


**Step-chain integrity:** Any code that mutates a service's `steps` array (wizard deploy, options reorder, import, auto-fix, HTTP step CRUD) must go through the relink helpers in `wizard-utils.js` and will be re-validated by `ServiceRegistry.save`/`validateChain`. Editing the array directly produces a service that saves but silently mis-executes.
